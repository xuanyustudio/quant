# 合约策略回测指南

## 🎯 功能说明

现在回测系统支持**两种策略模式**：

1. **现货策略**（spot）- 传统模式
2. **合约策略**（futures）- 真正的市场中性对冲

您可以通过 `--strategy` 参数选择使用哪种策略进行回测。

---

## 📊 两种策略对比

### 现货策略（spot）

```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31
```

**特点：**
- ❌ 卖出现货后无法从下跌中获利
- ❌ 有市场方向性风险
- ⚠️ 不是真正的市场中性策略

### 合约策略（futures）⭐ 推荐

```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31 \
  --strategy=futures
```

**特点：**
- ✅ 使用永续合约真正做空
- ✅ 完美对冲市场风险
- ✅ 真正的市场中性策略
- ✅ 空单可从下跌中获利

---

## 🚀 使用方法

### 基础命令

**现货策略回测**（默认）：
```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31
```

**合约策略回测**：
```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31 \
  --strategy=futures
```

**合约策略（指定杠杆）**：
```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31 \
  --strategy=futures \
  --leverage=1
```

---

## 📋 参数说明

| 参数 | 说明 | 是否必需 | 默认值 | 示例 |
|------|------|---------|--------|------|
| `--symbol1` | 第一个交易对 | ✅ 必需 | - | `BTC/USDT` |
| `--symbol2` | 第二个交易对 | ✅ 必需 | - | `ETH/USDT` |
| `--start` | 开始日期 | ✅ 必需 | - | `2024-10-01` |
| `--end` | 结束日期 | ✅ 必需 | - | `2024-10-31` |
| `--strategy` | 策略类型 | ⭕ 可选 | `spot` | `futures` 或 `spot` |
| `--leverage` | 杠杆倍数 | ⭕ 可选 | `1` | `1`、`2`、`5` |

---

## 💡 使用场景

### 场景1：对比两种策略的表现

```bash
# 1. 先用现货策略回测
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-09-01 \
  --end=2024-09-30 \
  --strategy=spot

# 记录结果：收益率 = X%

# 2. 再用合约策略回测（相同时间段）
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-09-01 \
  --end=2024-09-30 \
  --strategy=futures

# 记录结果：收益率 = Y%

# 3. 对比分析
#    - 哪种策略收益更稳定？
#    - 哪种策略最大回撤更小？
#    - 合约策略是否真正实现市场中性？
```

### 场景2：在下跌市场中的表现

```bash
# 选择一个市场大幅下跌的时间段
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-08-01 \
  --end=2024-08-15 \
  --strategy=futures

# 观察：
# - 合约策略是否通过空单对冲了下跌风险？
# - 总盈亏是否接近 0%（市场中性）？
```

### 场景3：测试不同杠杆倍数

```bash
# 1x 杠杆（推荐）
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31 \
  --strategy=futures \
  --leverage=1

# 2x 杠杆
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-10-01 \
  --end=2024-10-31 \
  --strategy=futures \
  --leverage=2

# 对比收益和风险
```

---

## 📈 回测报告示例

### 现货策略回测输出

```
════════════════════════════════════════════════════════════
📊 开始回测: BTC/USDT vs ETH/USDT
════════════════════════════════════════════════════════════
💵 策略类型: 现货策略（传统模式）
   做空方式: 卖出现货（非真正做空）⚠️

⏰ 回测开始时间: 2024-12-29 10:00:00
📅 数据时间范围: 2024-10-01 00:00:00 - 2024-10-31 23:59:59
📊 数据点数量: 2976 个
📈 相关系数: 0.895
💰 初始资金: 10000.00 USDT

...回测过程...

📊 回测结果摘要:
   收益率: 2.50%
   胜率: 65.0%
   夏普比率: 1.23
   最大回撤: -8.50%
   交易次数: 12
```

### 合约策略回测输出

```
════════════════════════════════════════════════════════════
📊 开始回测: BTC/USDT vs ETH/USDT
════════════════════════════════════════════════════════════
🔮 策略类型: 合约策略（市场中性对冲）
   杠杆倍数: 1x
   保证金模式: cross
   做空方式: 使用永续合约真正做空 ✅

⏰ 回测开始时间: 2024-12-29 10:00:00
📅 数据时间范围: 2024-10-01 00:00:00 - 2024-10-31 23:59:59
📊 数据点数量: 2976 个
📈 相关系数: 0.895
💰 初始资金: 10000.00 USDT

...回测过程...

📊 回测结果摘要:
   收益率: 3.80%
   胜率: 70.0%
   夏普比率: 1.85
   最大回撤: -3.20%  ← 更小的回撤！
   交易次数: 12
```

**关键差异：**
- ✅ 合约策略收益更高（3.80% vs 2.50%）
- ✅ 最大回撤更小（-3.20% vs -8.50%）
- ✅ 夏普比率更高（更好的风险调整后收益）

---

## 🔍 回测结果分析

### 关键指标

1. **收益率**
   - 现货：可能受市场方向影响
   - 合约：真正的价差回归收益

2. **最大回撤**
   - 现货：市场单边下跌时回撤大
   - 合约：完美对冲，回撤小 ✅

3. **夏普比率**
   - 现货：波动较大
   - 合约：风险调整后收益更好 ✅

4. **交易次数**
   - 两种策略相同（信号相同）

### 预期结果

**在正常市场（小幅波动）：**
- 两种策略表现接近
- 合约策略略优（真正的对冲）

**在单边市场（大幅涨跌）：**
- 现货策略：受市场方向影响大 ❌
- 合约策略：完美对冲，保持稳定 ✅

---

## ⚠️ 注意事项

### 1. 回测 ≠ 实盘

回测不包含：
- 资金费率（合约特有）
- 滑点损失
- 流动性风险
- 系统故障风险

### 2. 合约手续费

实盘合约交易需要考虑：
- 开仓手续费
- 平仓手续费
- 资金费率（每8小时）

回测中已包含基础手续费（0.1%），但资金费率未包含。

### 3. 杠杆风险

- **1x 杠杆**：安全，推荐 ✅
- **2x-5x**：适度风险
- **10x+**：高风险，不推荐 ❌

### 4. 数据质量

确保回测时间段：
- 有足够的数据点（建议 >1000）
- 包含不同市场状态（涨跌震荡）

---

## 📊 实际案例

### 案例1：BTC/ETH 配对（30天回测）

```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-09-01 \
  --end=2024-09-30 \
  --strategy=futures
```

**预期结果：**
- 收益率：2-5%（稳定）
- 胜率：65-75%
- 最大回撤：<5%
- 交易次数：10-15次

### 案例2：下跌市场测试

选择一个市场大幅下跌的时间段：

```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --start=2024-08-05 \
  --end=2024-08-10 \
  --strategy=futures
```

**关键观察：**
- 如果市场整体跌 20%
- 合约策略总盈亏应接近 0%（对冲成功）
- 现货策略可能亏损 10%+（对冲失败）

---

## 🎯 最佳实践

### 1. 对比测试

始终同时运行两种策略回测：

```bash
# 脚本1：现货策略
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31 \
  --strategy=spot

# 脚本2：合约策略
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31 \
  --strategy=futures
```

对比结果，选择更优策略。

### 2. 多时间段测试

```bash
# 测试不同市场环境
npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-07-01 --end=2024-07-31  # 牛市

npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-08-01 --end=2024-08-31  # 熊市

npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-09-01 --end=2024-09-30  # 震荡市
```

### 3. 多币对测试

```bash
# 测试不同相关性的币对
npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=BTC/USDT --symbol2=ETH/USDT  # 高相关

npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=BNB/USDT --symbol2=SOL/USDT  # 中相关

npm run stat-arb:backtest-pair -- --strategy=futures \
  --symbol1=ADA/USDT --symbol2=DOT/USDT  # 中低相关
```

---

## 💻 命令速查表

```bash
# 现货策略（默认）
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31

# 合约策略（1x杠杆）⭐ 推荐
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31 \
  --strategy=futures

# 合约策略（2x杠杆）
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31 \
  --strategy=futures --leverage=2

# 查看帮助
npm run stat-arb:backtest-pair -- --help
```

---

## 📚 相关文档

- [合约策略完整指南](./FUTURES_STRATEGY_GUIDE.md)
- [现货vs合约对比](../FUTURES_STRATEGY_UPDATE.md)
- [统计套利指南](./STATISTICAL_ARBITRAGE_GUIDE.md)

---

## 🎉 总结

**使用合约策略回测的优势：**

1. ✅ **真实对冲效果**：测试真正的市场中性策略
2. ✅ **更准确的评估**：反映实盘合约交易的真实表现
3. ✅ **风险量化**：看到在不同市场环境下的真实风险
4. ✅ **策略对比**：直观对比现货vs合约的差异

**推荐工作流：**
```
1. 回测现货策略（了解基准）
     ↓
2. 回测合约策略（对比改进）
     ↓
3. 选择更优策略
     ↓
4. 多时间段验证
     ↓
5. 小资金实盘测试
```

**开始回测吧！🚀**

```bash
npm run stat-arb:backtest-pair -- \
  --symbol1=BTC/USDT --symbol2=ETH/USDT \
  --start=2024-10-01 --end=2024-10-31 \
  --strategy=futures
```

