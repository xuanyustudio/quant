# 配对交易自动下单使用指南

## 📊 策略原理

### 什么是配对交易（统计套利）？

配对交易是一种**市场中性策略**，通过同时做多和做空两个相关的币种来套利：

1. **价差偏离时开仓**（一多一空）
   - 当两个币种价格关系偏离正常范围时
   - 买入被**低估**的币种
   - 卖出被**高估**的币种

2. **价差回归时平仓**（锁定利润）
   - 当价差回归到正常范围
   - 同时平掉多单和空单
   - 实现套利利润

### 举例说明

假设 BTC 和 ETH 通常保持稳定的价格比率关系：

**开仓场景：价差偏离**
- 正常情况：BTC/ETH 比率在一个稳定范围
- 突然：ETH 相对 BTC 被严重高估（Z-Score > 2）
- **策略行动**：
  - ✅ 卖出 ETH（做空高估币）
  - ✅ 买入 BTC（做多低估币）
  - 💰 投入资金：两边各 $500（总共 $1000）

**平仓场景：价差回归**
- 几小时后：ETH/BTC 回归正常关系（Z-Score 接近 0）
- **策略行动**：
  - ✅ 买入 ETH（平空单）
  - ✅ 卖出 BTC（平多单）
  - 💰 **锁定利润**

### 关键优势

1. **市场中性**：不管整体市场涨跌，只要价差回归就盈利
2. **风险对冲**：一个币涨另一个跌，整体风险较低
3. **统计依据**：基于历史数据的协整关系，有统计学支撑

---

## 🚀 启用自动交易

### ⚠️ 重要提示

**在启用自动交易前，请务必：**

1. ✅ 已经在测试环境充分回测
2. ✅ 理解配对交易的原理和风险
3. ✅ 从小资金开始（建议 $100-$500）
4. ✅ 监控前几笔交易的执行情况
5. ✅ 设置好止损参数

### 配置方法

#### 方法1：修改实盘配置文件

在 `live-trading.js` 中找到策略配置部分：

```javascript
const strategyConfig = {
  // ... 其他配置 ...
  
  // ⭐ 启用自动交易（默认 false）
  autoTrade: true,  // 改为 true
  
  // 交易金额（建议从小开始）
  tradeAmount: 200,  // 每个配对使用 $200
  
  // ... 其他参数 ...
};
```

#### 方法2：通过环境变量

在 `.env` 文件中添加：

```bash
# 启用自动交易
AUTO_TRADE=true

# 单笔交易金额
TRADE_AMOUNT=200
```

---

## 📝 交易执行流程

### 1️⃣ 开仓流程

当检测到交易信号时：

```
检测到价差偏离（Z-Score > 2 或 < -2）
    ↓
生成交易信号（OPEN_LONG 或 OPEN_SHORT）
    ↓
【策略层】记录持仓信息到内存
    ↓
【执行层】提交真实订单到交易所
    ├─ 币种1：市价买入/卖出
    └─ 币种2：市价卖出/买入
    ↓
✅ 两腿订单均成交 → 开仓完成
```

### 2️⃣ 平仓流程

当价差回归时：

```
检测到价差回归（Z-Score 接近 0）
    ↓
生成平仓信号（CLOSE）
    ↓
【策略层】更新持仓状态为 CLOSED
    ↓
【执行层】提交平仓订单到交易所
    ├─ 币种1：市价卖出/买入（反向）
    └─ 币种2：市价买入/卖出（反向）
    ↓
✅ 平仓完成 → 计算盈亏
```

---

## 🔍 关键代码说明

### 开仓逻辑

```javascript
// 做多价差（当价差偏低）
if (signal.action === 'OPEN_LONG') {
  // 买入低估币种（symbol1）
  await exchange.createOrder(symbol1, 'market', 'buy', quantity1);
  
  // 卖出高估币种（symbol2）
  await exchange.createOrder(symbol2, 'market', 'sell', quantity2);
}

// 做空价差（当价差偏高）
if (signal.action === 'OPEN_SHORT') {
  // 卖出高估币种（symbol1）
  await exchange.createOrder(symbol1, 'market', 'sell', quantity1);
  
  // 买入低估币种（symbol2）
  await exchange.createOrder(symbol2, 'market', 'buy', quantity2);
}
```

### 平仓逻辑

```javascript
// 平仓时执行反向操作
if (position.type === 'OPEN_LONG') {
  // 卖出 symbol1，买入 symbol2
  await exchange.createOrder(symbol1, 'market', 'sell', quantity1);
  await exchange.createOrder(symbol2, 'market', 'buy', quantity2);
} else {
  // 买入 symbol1，卖出 symbol2
  await exchange.createOrder(symbol1, 'market', 'buy', quantity1);
  await exchange.createOrder(symbol2, 'market', 'sell', quantity2);
}
```

---

## ⚙️ 重要参数说明

### 交易参数

| 参数 | 说明 | 推荐值 | 风险等级 |
|------|------|--------|----------|
| `autoTrade` | 是否启用自动交易 | `false` | - |
| `tradeAmount` | 单个配对的交易金额 | $200 | 低资金=低风险 |
| `maxPositions` | 最大同时持仓数 | 3 | 分散风险 |

### 策略参数

| 参数 | 说明 | 推荐值 | 影响 |
|------|------|--------|------|
| `entryThreshold` | 开仓阈值（Z-Score） | 2.0-3.0 | 越高=越保守 |
| `exitThreshold` | 平仓阈值（Z-Score） | 0.5-1.0 | 越低=越快平仓 |
| `stopLossThreshold` | 止损阈值（Z-Score） | 4.0-5.0 | 越低=越早止损 |
| `minCorrelation` | 最小相关系数 | 0.7-0.8 | 越高=越相关 |

---

## ⚠️ 风险控制

### 1. 资金管理

```javascript
// 建议配置
const riskConfig = {
  // 单个配对最多使用总资金的 10%
  maxPositionSize: 0.1,
  
  // 最多同时 3 个配对（总共 30% 资金使用率）
  maxPositions: 3,
  
  // 单日最大亏损 5%
  maxDailyLoss: 0.05,
  
  // 最大回撤 15%
  maxDrawdown: 0.15
};
```

### 2. 订单执行风险

**问题：两腿订单只成交一个**

- **原因**：网络延迟、余额不足、市场流动性
- **解决**：
  - ✅ 系统会自动检测并提示
  - ⚠️ 需要手动检查账户并处理

**建议**：
- 使用流动性好的交易对（如 BTC/ETH）
- 确保账户有足够余额（包括手续费）
- 监控前几笔交易的执行情况

### 3. 市场风险

**价差持续偏离（不回归）**

- **止损保护**：当 Z-Score 超过 `stopLossThreshold` 时自动止损
- **建议**：定期检查配对的协整关系是否仍然有效

---

## 📈 监控与日志

### 日志位置

所有交易日志记录在：

```
logs/
  ├── combined.log     # 完整日志
  ├── error.log        # 错误日志
  └── trades.log       # 交易记录
```

### 关键日志标识

```
🔄 开始执行开仓订单...    # 开始下单
✅ 订单已成交              # 订单成功
❌ 开仓失败                # 订单失败
📊 交易类型: 做多/做空价差  # 交易方向
✅ 开仓完成！两腿订单均已执行 # 完全成功
```

---

## 🧪 测试流程

### 第一阶段：回测验证

```bash
# 先在历史数据上回测
npm run stat-arb:backtest-single -- \
  --symbol1=BTC/USDT \
  --symbol2=ETH/USDT \
  --days=30
```

### 第二阶段：监控模式（不下单）

```javascript
// 设置 autoTrade: false
const strategyConfig = {
  autoTrade: false,  // 只监控，不下单
  // ...
};
```

观察信号质量和频率，运行 1-3 天。

### 第三阶段：小资金实盘

```javascript
// 启用自动交易，小资金
const strategyConfig = {
  autoTrade: true,
  tradeAmount: 100,  // 从 $100 开始
  // ...
};
```

运行 3-7 天，观察实际盈亏。

### 第四阶段：正常运营

确认策略稳定后，逐步增加资金。

---

## 🔧 故障排查

### 问题1：订单未执行

**检查清单：**
1. ✅ `autoTrade` 是否设为 `true`
2. ✅ API 密钥是否有交易权限
3. ✅ 账户余额是否充足
4. ✅ 交易对是否支持交易

### 问题2：只有一个订单成交

**立即行动：**
1. 🚨 检查交易所账户实际持仓
2. 🔍 查看日志确认哪个订单失败
3. ⚙️ 手动执行另一个订单以对冲风险

### 问题3：频繁止损

**可能原因：**
- 配对的协整关系已经失效
- `entryThreshold` 设置过低（信号质量差）
- 市场波动过大

**解决方案：**
- 提高 `entryThreshold`（如从 2.0 提高到 3.0）
- 检查配对的历史相关性
- 考虑更换配对

---

## 📞 紧急停止

如需立即停止自动交易：

### 方法1：停止程序

```bash
# 按 Ctrl+C 停止程序
```

### 方法2：临时禁用

```javascript
// 修改配置
autoTrade: false
```

### 方法3：修改环境变量

```bash
# 在 .env 中
AUTO_TRADE=false
```

⚠️ **重要**：停止程序不会自动平仓现有持仓，需要手动处理！

---

## 📚 相关文档

- [统计套利完整指南](./STATISTICAL_ARBITRAGE_GUIDE.md)
- [配对交易策略](./STRATEGIES.md)
- [风险控制](./PRE_LIVE_CHECKLIST.md)

---

## 💡 最佳实践

1. **从小做起**：先用 $100-$200 测试
2. **持续监控**：前几天密切关注交易执行
3. **及时止损**：发现问题立即停止
4. **记录分析**：保存每笔交易的详细记录
5. **定期优化**：根据实际表现调整参数

---

## ⚖️ 免责声明

**加密货币交易存在高风险，可能损失全部本金。**

- 本系统仅供学习和研究使用
- 不构成任何投资建议
- 使用者需自行承担所有交易风险
- 建议先用小额资金充分测试

**请在充分理解风险的前提下使用自动交易功能。**

